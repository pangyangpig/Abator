==========================================
    Create database ORM model for java                             
==========================================

#   usage
--------------

$ mvn clean compile jar:jar -X
$ cd target
Copy config.properties and mysql driver for java jar to target
$ java -classpath ./db-tools-java-X.X.X.jar \
  com.xiebiao.tools.db.Creator 
  
lookup output current directory 
